import 'package:flutter/material.dart';

/// Paleta oficial da marca "Rifon no Telon".
///
/// Usada como cor padrão em toda a interface (fundo, números, texto
/// secundário) e nos detalhes fixos das telas de ajuda/configuração que
/// não fazem parte da customização de cores do usuário.
class BrandColors {
  BrandColors._();

  static const darkGreen = Color(0xFF2F3A2E);
  static const wine = Color(0xFF7A1E1E);
  static const gold = Color(0xFFC7A26A);
  static const olive = Color(0xFF8A8F6A);
  static const cream = Color(0xFFF3EFE6);
}
